# Everything the build regenerates or must never reach an image.
node_modules
**/node_modules
.next
**/.next
.turbo
**/.turbo
dist
build
out
coverage

# Secrets. The image is built once and may be pushed to a registry; .env is
# per-server and arrives through docker compose at run time instead.
.env
.env.*
!.env*.example

.git
.github
docs
.media
*.log
*.tsbuildinfo
.DS_Store
.vscode
.idea
